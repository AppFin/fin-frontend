export interface ITranslate {
  'fin-core': {
    actions: {
      save: string;
    };
    errors: {
      email: string;
      maxLength: string;
      minLength: string;
      pattern: string;
      required: string;
    };
  };
}
