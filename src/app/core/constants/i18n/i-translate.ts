export interface ITranslate {
  'fin-core': {
    actions: {
      loading: string;
      save: string;
    };
    errors: {
      email: string;
      maxLength: string;
      minLength: string;
      pattern: string;
      required: string;
    };
    grid: {
      sample: {
        item: string
      }
    }
  };
}
