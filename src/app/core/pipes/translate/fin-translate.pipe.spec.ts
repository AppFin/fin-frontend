import { FinTranslatePipe } from './fin-translate.pipe';

describe('FinTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new FinTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
