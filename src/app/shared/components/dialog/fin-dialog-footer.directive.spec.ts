import { FinDialogFooterDirective } from './fin-dialog-footer.directive';

describe('FinDialogFooterDirective', () => {
  it('should create an instance', () => {
    const directive = new FinDialogFooterDirective();
    expect(directive).toBeTruthy();
  });
});
