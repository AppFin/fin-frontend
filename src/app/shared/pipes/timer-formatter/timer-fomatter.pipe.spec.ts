import { TimerFomatterPipe } from './timer-fomatter.pipe';

describe('TimerFomatterPipe', () => {
  it('create an instance', () => {
    const pipe = new TimerFomatterPipe();
    expect(pipe).toBeTruthy();
  });
});
